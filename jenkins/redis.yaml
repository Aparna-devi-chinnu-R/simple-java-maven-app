redisData:
  - name : ${Environment}
    url : $Environment
    alias : "{env.Environment}"
    numberOfReplicas : ${env.ENVIRONMENT}
  - name : $env.ENVIRONMENT
    url : env.ENVIRONMENT
    alias : ENVIRONMENT
    numberOfReplicas : $ENVIRONMENT
  - name : "ENVIRONMENT"
    url : ${ENVIRONMENT}
    alias : "{ENVIRONMENT}"
    numberOfReplicas : '${ENVIRONMENT}'

    
